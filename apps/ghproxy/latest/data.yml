additionalProperties:
  formFields:
    - default: 7210                     # 默认值
      edit: true                        # 是否可被编辑
      envKey: PANEL_APP_PORT_HTTP       # 映射到的环境变量键名
      labelEn: Port                     
      labelZh: 端口                      
      required: true                    # 是否为必填项
      rule: paramPort                   # 校验规则
      type: number                      # 字段类型

    - default: ./data/log               # 默认值
      edit: true                        # 是否可被编辑
      envKey: LOG_PATH                  # 映射到的环境变量键名
      labelEn: log path         
      labelZh: 数据文件夹路径            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型

    - default: ./data/config            # 默认值
      edit: true                        # 是否可被编辑
      envKey: CONFIG_PATH               # 映射到的环境变量键名
      labelEn: config path         
      labelZh: 数据文件夹路径            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型