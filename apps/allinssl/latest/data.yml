additionalProperties:
  formFields:
    - default: 7979                     # 默认值
      edit: true                        # 是否可被编辑
      envKey: PANEL_APP_PORT_HTTP       # 映射到的环境变量键名
      labelEn: Port                     
      labelZh: 端口                      
      required: true                    # 是否为必填项
      rule: paramPort                   # 校验规则
      type: number                      # 字段类型

    - default: ./data                   # 默认值
      edit: true                        # 是否可被编辑
      envKey: DATA_PATH                 # 映射到的环境变量键名
      labelEn: Data folder path         
      labelZh: 数据文件夹路径            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型

    - default: allinssl                 # 默认值
      edit: true                        # 是否可被编辑
      envKey: ALLINSSL_USER             # 映射到的环境变量键名
      labelEn: suername         
      labelZh: 用户名            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型

    - default: allinssldocker           # 默认值
      edit: true                        # 是否可被编辑
      envKey: ALLINSSL_PWD              # 映射到的环境变量键名
      labelEn: password         
      labelZh: 密码            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型

    - default: allinssl                 # 默认值
      edit: true                        # 是否可被编辑
      envKey: ALLINSSL_URL              # 映射到的环境变量键名
      labelEn: Secure access path         
      labelZh: 安全访问路径            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型

    - default: Asia/Shanghai            # 默认值
      edit: true                        # 是否可被编辑
      envKey: ALLINSSL_TZ               # 映射到的环境变量键名
      labelEn: time zone         
      labelZh: 时区            
      required: true                    # 是否为必填项：是
      type: text                        # 字段类型